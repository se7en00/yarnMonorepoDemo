describe("@Router()", () => {
    it("should set request method on GET by default", () => {
        // class Test {
        //     @Router({ method: "GET", path: "/list" })
        //     public static test() {}
        // }
        // const method = Metadata.getType(Test, "test")
        // expect(method).toBe()
    })
})
